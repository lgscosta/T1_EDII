#ifndef quick_h_
#define quick_h_

// The parameters are: list of ints, first index, last index and counters list
// The last one will be filled inside 
void quick_sort(int*, int, int, float*);

#endif /* quick_h_ */