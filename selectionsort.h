#ifndef selection_h_
#define selection_h_

// The parameters are: list of ints, size of list and counters list
// The last one will be filled inside 
void selection_sort(int*, int, float*);

#endif /* selection_h_ */